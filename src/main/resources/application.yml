server:
  port: 3000

spring:
  application:
    name: order-service

pactbroker:
  url: ${PACT_BROKER_URL:https://pact-mock.pactflow.io}
  token: ${PACT_BROKER_TOKEN}

inventory:
  service:
    url: http://localhost:4000/v1